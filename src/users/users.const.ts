export enum UsersExceptionsEnum {
  INVALID_CREDENTIALS = 'Invalid Credentials',
  EMAIL_EXISTS = 'Email exists already',
  USERNAME_SLUG = 'Username must be a valid slugs',
  NAME_SPECIALS_CHARACTERS = 'Name must not have special characters',
  NAME_MUST_DIFFERENT = 'Name must be different',
  USERNAME_MUST_DIFFERENT = 'Username must be different',
}
